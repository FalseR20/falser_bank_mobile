<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
        xmlns:tools="http://schemas.android.com/tools">

    <application
            android:allowBackup="true"
            android:dataExtractionRules="@xml/data_extraction_rules"
            android:fullBackupContent="@xml/backup_rules"
            android:icon="@mipmap/ic_launcher"
            android:label="@string/app_name"
            android:roundIcon="@mipmap/ic_launcher_round"
            android:supportsRtl="true"
            android:theme="@style/Theme.FalseRBank"
            tools:targetApi="31">
        <activity
                android:name=".MainActivity"
                android:exported="true"
                android:label="@string/title_activity_main"
                android:screenOrientation="portrait"
                tools:ignore="LockedOrientationActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
                android:name=".ui.cards.new_card.NewCardActivity"
                android:exported="false"
                android:label="@string/title_activity_new_card" />
        <activity
                android:name=".ui.cards.transactions.transfer.TransferActivity"
                android:exported="false"
                android:label="@string/title_transfer" />
        <activity
                android:name=".ui.cards.transactions.transfer.DepositActivity"
                android:exported="false"
                android:label="@string/title_deposit" />

    </application>

</manifest>